# M4: Workspace Integration & Documentation

**Epic:** [Epic 4 — Polish](../epic-4-polish.md)
**Priority:** P1
**Depends on:** All other tickets

## Description

Integrate `clhorde-web` into the project workspace, CI pipeline, release builds, and documentation.

## Tasks

- [ ] Add `clhorde-web` to workspace members in root `Cargo.toml`
- [ ] Update CI workflow to build `clhorde-web` binary alongside existing binaries
- [ ] Add `clhorde-web` to release binary upload workflow (Linux + macOS)
- [ ] Update `CLAUDE.md` architecture section:
  - Add `clhorde-web` crate description and file layout
  - Add `clhorde-web` binary to the binary names table
  - Add web-specific dependencies to the dependency section
- [ ] Add web interface section to `docs/` site:
  - REST API reference
  - WebSocket API reference
  - Getting started guide (start daemon, start web, open browser)
- [ ] Add `--web` mention to existing daemon/CLI documentation
- [ ] Add systemd user service example for `clhorde-web` (similar to existing `clhorded` service)

## Acceptance Criteria

- `cargo build --release` builds all 4 binaries
- CI builds and uploads `clhorde-web` in release workflow
- CLAUDE.md accurately reflects the new crate
- User-facing docs cover setup and API reference
