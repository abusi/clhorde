All apply-phase sections of OpenSpec change `{{ change_name }}` are complete.

Verify the work by running the project's test suite end-to-end. If anything
fails, identify and fix it, then rerun until the suite is green. When the
test suite passes cleanly, stop — do not modify {{ change_dir }}/tasks.md
beyond what's already done.
