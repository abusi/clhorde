Run `/opsx:propose {{ idea }}` to draft a new OpenSpec change.

Once the change directory `openspec/changes/<name>/` exists with proposal.md,
design.md, specs/, and tasks.md, stop. Do not begin implementation — that
runs as a separate workflow once the user queues the change.
